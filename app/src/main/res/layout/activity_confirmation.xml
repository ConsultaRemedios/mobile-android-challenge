<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ConfirmationActivity">

    <ImageView
        android:id="@+id/imageViewBag"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginTop="81dp"
        android:src="@drawable/ic_icbag"
        android:tint="@color/colorGreen"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textViewAhoooy"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="12dp"
        android:text="AHOOOY!!"
        android:textColor="@color/colorGreen"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/imageViewBag" />

    <TextView
        android:id="@+id/textViewOrderDetails"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="24dp"
        android:paddingTop="16dp"
        android:paddingRight="24dp"
        android:paddingBottom="16dp"
        android:text="Sua compra Nº 011219 foi concluída com sucesso. Em breve você receberá seus produtos."
        android:textAlignment="center"
        android:textColor="@color/colorSecondaryText"
        android:textSize="14sp"
        app:layout_constraintTop_toBottomOf="@id/textViewAhoooy" />

    <TextView
        android:id="@+id/continuarComprando"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:clickable="true"
        android:focusable="true"
        android:text="Continuar Comprando"
        android:textColor="@color/colorBlue"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/textViewOrderDetails" />

</androidx.constraintlayout.widget.ConstraintLayout>