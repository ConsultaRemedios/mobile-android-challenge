<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.challange.crandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ConfirmationActivity"
            android:label="Compra Concluída"
            android:parentActivityName=".GamesActivity"></activity>
        <activity
            android:name=".CheckoutActivity"
            android:label="Carrinho de Compras"
            android:launchMode="singleTop"
            android:parentActivityName=".GameDetailActivity"></activity>
        <activity
            android:name=".GameDetailActivity"
            android:label="@string/empty"
            android:launchMode="singleTop"
            android:parentActivityName=".GamesActivity"></activity>
        <activity
            android:name=".GamesActivity"
            android:label="@string/games"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>